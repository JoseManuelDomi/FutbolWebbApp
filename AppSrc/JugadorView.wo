// C:\DataFlex Projects\FutbolWebbApp\AppSrc\JugadorView.wo
// Ficha del Jugador
//

Use cWebView.pkg
Use cWebPanel.pkg
Use cWebForm.pkg

Use cposicionDataDictionary.dd
Use cjugadorDataDictionary.dd

Object oJugadorView is a cWebView
    Set piColumnCount to 12
    Set psCaption to "Ficha del Jugador"
    Set piWidth to 700

    Object oposicion_DD is a cposicionDataDictionary
    End_Object 

    Object ojugador_DD is a cjugadorDataDictionary
        // Eliminado el DDO_Server porque NO hay relación en la base de datos
    End_Object 

    Set Main_DD to ojugador_DD
    Set Server  to ojugador_DD

    Object oWebMainPanel is a cWebPanel
        Set piColumnCount to 12

        Object ojugadorID is a cWebForm
            Entry_Item jugador.ID
            Set piColumnSpan to 2
            Set piColumnIndex to 0
            Set peLabelPosition to lpTop
            Set psLabel to "ID Jugador"
        End_Object 

        Object ojugadorNombre is a cWebForm
            Entry_Item jugador.Nombre
            Set piColumnSpan to 5
            Set piColumnIndex to 0
            Set peLabelPosition to lpTop
            Set psLabel to "Nombre"
        End_Object 

        Object ojugadorApellido is a cWebForm
            Entry_Item jugador.Apellido
            Set piColumnSpan to 5
            Set piColumnIndex to 0
            Set peLabelPosition to lpTop
            Set psLabel to "Apellido"
        End_Object 

        // --- INICIO CÓDIGO PUNTO 4: LUPA HACIA POSICIONES ---
        Object oposicionID is a cWebForm
            // TRUCO: Al quitar el Entry_Item, el campo no busca en la BBDD
            // y no da Error 4306. Es un campo independiente.
            
            Set piColumnSpan to 4
            Set piColumnIndex to 0
            Set peLabelPosition to lpTop
            Set psLabel to "ID Posición (Sin relación real)"
            
            Set pbPromptButton to True 
            
            // Asumiendo que tu archivo de lista se llama oPosicionSelect
            WebRegisterPath ntNavigateForward oPosicionSelect 

            Procedure OnPrompt
                Send NavigatePath
            End_Procedure
        End_Object 
        // --- FIN CÓDIGO PUNTO 4 ---
        
    End_Object 

End_Object